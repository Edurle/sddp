# === 数据库 ===
DATABASE_URL=postgresql+asyncpg://pg_user:pg_password@pg_host:5432/sdd_db

# === JWT ===
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_HOURS=8
REMEMBER_TOKEN_EXPIRE_DAYS=30

# === 管理员初始账号 ===
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!
ADMIN_NICKNAME=管理员

# === 可选：SMTP ===
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
